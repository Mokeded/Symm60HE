# CAD and Fusion handoff tools

These commands prepare the PCB-derived reference geometry, export STEP/STL
models through FreeCAD, and verify the resulting Fusion 360 handoff files.
They are run in the required order by the repository-level `build.sh`.
