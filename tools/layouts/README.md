# Layout variant tools

These commands derive, materialize, repair, and verify the fixed-layout PCB
variants from the routed universal masters. They are separate from general PCB
editing so layout permutations and their manufacturing checks are easy to find.
