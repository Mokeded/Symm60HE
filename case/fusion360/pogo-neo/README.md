# Retired detached preview

The former detached Neo preview used the obsolete 20 x 20 mm spring-head
assumption and a separate coordinate system. Use
`../Symm60HE-case-reference-assembly.step` instead. That master assembly
contains the current 20 x 6 mm routed heads and all pogo/FFC connector solids
in their actual in-case positions.
